#include<stdio.h>
#include<stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch = 0;
for (ch = 0; ch <= 15; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
