#include<stdio.h>
#include<stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch = '0';
while (ch <= 15)
{
putchar(ch);
ch++;
}
putchar('\n');
return (0);
}
